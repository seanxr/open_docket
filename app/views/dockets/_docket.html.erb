<li>
  <span class="content">
    <%= link_to docket.item.number, item_path(docket.item.id) %> | 
                   <%= link_to "Remove", [docket.item, docket], method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: "Title" %>
   </span>
  <span class="timestamp">
    <%= docket.created_at.strftime("Added %m/%d/%y") %>
  </span>
</li>
